name              = "cms-events"
main              = "index.js"
compatibility_date = "2025-01-01"

[vars]
SITE_URL   = "https://anand-raj.github.io"
ADMIN_URL  = "https://admin-portal-93k.pages.dev"

# After creating the D1 database with:
#   npx wrangler d1 create cms-events
# paste the database_id below, then run:
#   npx wrangler d1 execute cms-events --file=schema.sql --remote

# Single consolidated CMS database
[[d1_databases]]
binding       = "DB"
database_name = "cms"
database_id   = "3d9706db-0a45-4c18-ac40-3b477cb0c915"
